name: Patch pitching algorithm

on:
  push:
    paths:
      - .github/workflows/patch-pitching-algorithm.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Patch real app source
        shell: python
        run: |
          from pathlib import Path
          p=